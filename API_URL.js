import Constants from 'expo-constants';

const {manifest} = Constants;

// const uri = `http://${manifest.debuggerHost.split(':').shift()}:4001`;
const uri = 'http://192.168.100.167:4001';

export const API_URL = uri;
