import React from 'react';
import StreamContainer from '../Streams/Components/StreamContainer';

export const RecentFriends = ({navigation}) => {
  return (
    <>
      <StreamContainer navigation={navigation} />
    </>
  );
};
