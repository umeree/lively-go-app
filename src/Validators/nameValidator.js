export function firstNameValidator(name) {
  if (!name) return "First Name can't be empty.";
  return "";
}

export function lastNameValidator(name) {
  if (!name) return "Last name can't be empty.";
  return "";
}
